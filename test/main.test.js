'use strict';

const fs = require('fs');
const path = require('path');
const assert = require('assert');
const O = require('..');

const cwd = __dirname;

describe('Omikron', () => {
  it('Works', () => {
    // ok
  });
});